import React from 'react';
import {Head, Main, NextScript} from 'next/document';

const layout = () => (
  <html>
    <Head />
    <body>
      <div>〜〜〜全体のレイアウト〜〜〜</div>
      <Main />
      <NextScript />
    </body>
  </html>
);

export default layout;
