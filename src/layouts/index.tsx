import React from 'react';
import {Head, Main, NextScript} from 'next/document';

const layout = () => (
  <html>
    <Head />
    <body>
      <Main />
      <NextScript />
    </body>
  </html>
);

export default layout;
